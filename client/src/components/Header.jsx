import { React } from "react";


const Header = (props) => {
    return (
        <div>
        {/* Header */}
            <header id="header">
                <h1 className="logo">Logic Gate Generator</h1>
            </header>
        </div>
    );
};

export default Header;
